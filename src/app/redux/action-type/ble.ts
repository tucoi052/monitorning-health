import { SLICE_NAME } from '@config/type';

export const SCAN = SLICE_NAME.BLE + 'SCAN';

export const DISCOVER_DEVICE = SLICE_NAME.BLE + 'DISCOVER_DEVICE';

export const CONNECT_DEVICE = SLICE_NAME.BLE + 'CONNECT_DEVICE';


